import { useState } from 'react';
import { useLocation } from 'wouter';
import { ChevronDown,ChevronUp,Eye,EyeOff } from 'lucide-react';
import { useAuth } from '@/contexts/AuthContext';
import { Button } from '@/components/ui/button';import { Input } from '@/components/ui/input';
export default function StoreLogin(){const{signIn}=useAuth();const[,setLocation]=useLocation();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[show,setShow]=useState(false);const[open,setOpen]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState('');const[form,setForm]=useState({companyName:'',fullName:'',city:'',district:'',phone:'',businessCategory:''});const field=(k:keyof typeof form,v:string)=>setForm(s=>({...s,[k]:v}));const login=async()=>{if(!email.trim()||!password){setError('E-posta ve şifre girin.');return;}setBusy(true);setError('');try{await signIn(email,password);setLocation('/hesabim');}catch(e){setError(e instanceof Error?e.message:'E-posta veya şifre hatalı.');}finally{setBusy(false);}};return <div className="min-h-screen bg-[#f6f7f9] px-4 py-10 flex items-center justify-center"><div className="w-full max-w-md rounded-2xl border bg-white p-6 md:p-8 shadow-sm"><div className="text-center"><img src="/api/app-branding/admin/logo" className="mx-auto max-h-24 max-w-[220px] object-contain"/><h1 className="mt-5 text-2xl font-bold">Çalışkan B2B</h1><p className="mt-1 text-sm text-slate-500">Toptan satın alma hesabınıza giriş yapın</p></div><div className="mt-7 space-y-4"><label className="block text-sm font-medium">E-posta<Input className="mt-2" value={email} onChange={e=>setEmail(e.target.value)} placeholder="ornek@caliskangroup.com" type="email"/></label><label className="block text-sm font-medium">Şifre<div className="relative mt-2"><Input value={password} onChange={e=>setPassword(e.target.value)} type={show?'text':'password'} placeholder="••••••••" className="pr-11"/><button type="button" onClick={()=>setShow(v=>!v)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-500">{show?<EyeOff className="h-4 w-4"/>:<Eye className="h-4 w-4"/>}</button></div></label><button className="block ml-auto text-sm font-semibold text-slate-900 hover:underline">Şifremi Unuttum</button>{error&&<div className="rounded-lg bg-red-50 px-3 py-2 text-sm text-red-700">{error}</div>}<Button className="w-full h-11 bg-slate-950 hover:bg-slate-800" disabled={busy} onClick={login}>{busy?'Giriş yapılıyor…':'Giriş Yap'}</Button><button onClick={()=>setOpen(v=>!v)} className="w-full flex items-center justify-center gap-1 py-2 text-sm font-semibold">{open?'Üyelik Formunu Kapat':'Hemen Üye Ol'}{open?<ChevronUp className="h-4 w-4"/>:<ChevronDown className="h-4 w-4"/>}</button><div className={`grid transition-all duration-300 ${open?'grid-rows-[1fr] opacity-100':'grid-rows-[0fr] opacity-0'}`}><div className="overflow-hidden"><div className="space-y-3 border-t pt-5"><div><h2 className="font-bold">Toptan Satış Üyeliği</h2><p className="text-sm text-slate-500">Firmanıza ait bilgileri girerek Çalışkan B2B üyeliğinizi oluşturun.</p></div><Input placeholder="Firma İsmi" value={form.companyName} onChange={e=>field('companyName',e.target.value)}/><Input placeholder="İsim Soy İsim" value={form.fullName} onChange={e=>field('fullName',e.target.value)}/><div className="grid grid-cols-2 gap-2"><Input placeholder="İl" value={form.city} onChange={e=>field('city',e.target.value)}/><Input placeholder="İlçe" value={form.district} onChange={e=>field('district',e.target.value)}/></div><Input placeholder="Telefon Numarası" value={form.phone} onChange={e=>field('phone',e.target.value)}/><Input placeholder="İşletme Kategorisi" value={form.businessCategory} onChange={e=>field('businessCategory',e.target.value)}/><Button variant="outline" className="w-full" onClick={()=>setLocation('/kaydol')}>Üyeliğe Devam Et</Button></div></div></div></div><p className="mt-8 text-center text-[11px] tracking-widest text-slate-400">POWERED BY Orvian</p></div></div>}
