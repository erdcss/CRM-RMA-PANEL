import {createContext,useContext,useEffect,useMemo,useState,type ReactNode} from 'react';import{localAuth,type AuthSession,type AuthUser}from'@/lib/supabase';import{rmaApi}from'@/lib/api';
type V={session:AuthSession|null;user:AuthUser|null;loading:boolean;signIn:(e:string,p:string)=>Promise<void>;signUp:(e:string,p:string)=>Promise<void>;signOut:()=>Promise<void>;deleteAccount:()=>Promise<void>};const C=createContext<V|undefined>(undefined);
export function AuthProvider({children}:{children:ReactNode}){const[session,setSession]=useState<AuthSession|null>(null),[loading,setLoading]=useState(true);useEffect(()=>{localAuth.getSession().then(setSession).finally(()=>setLoading(false));},[]);const value=useMemo<V>(()=>({session,user:session?.user??null,loading,async signIn(e,p){setSession(await localAuth.signIn(e,p));},async signUp(e,p){setSession(await localAuth.signIn(e,p));},async signOut(){await localAuth.signOut();setSession(null);},async deleteAccount(){await rmaApi.deleteAccount();await localAuth.signOut();setSession(null);}}),[session,loading]);return <C.Provider value={value}>{children}</C.Provider>}export function useAuth(){const c=useContext(C);if(!c)throw new Error('useAuth must be used within AuthProvider');return c;}
