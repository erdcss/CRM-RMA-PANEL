import AsyncStorage from "@react-native-async-storage/async-storage";const API=(process.env.EXPO_PUBLIC_API_URL??"").replace(/\/$/,""),KEY="caliskan_business_token";
export async function signIn(email:string,password:string){if(!API)throw new Error("EXPO_PUBLIC_API_URL tanımlı değil");const r=await fetch(`${API}/api/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const p=await r.json();if(!r.ok)throw new Error(p.error||"Giriş başarısız");if(p.user?.appAccess!=="business"&&!["admin","super_admin"].includes(p.user?.role)){throw new Error("Bu hesabın Çalışkan Business erişimi yok");}await AsyncStorage.setItem(KEY,p.token);return p;}